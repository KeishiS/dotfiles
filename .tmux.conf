set-option -g default-terminal screen-256color
set -g terminal-overrides 'xterm:colors=256'

setw -g status-style fg=color7,bg=color0

set -g prefix C-z
unbind C-b
bind h split-window -v
bind v split-window -h
